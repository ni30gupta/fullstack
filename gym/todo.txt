1. to be discuss if user chooses two body part then the weightage should be 0.5 or 1
   same way if chooses mix , then all body part will get 1/total_body_count say 0.2 each
2. add geo fencing,in checkin, also track if already checkid in, in backend,

3. map user to single device, means only device can be logged in at a time

4. apply auto logout if time exceeded to 100 minutes, or prefreed time range*1.5 
5. add updates tab in bottom bar, to showcase general notification or update from gym t0 users







features:
Attendance - record
track fees pending
send fees reminder
time based billing - pay as you use


import firebase_admin
from firebase_admin import credentials

cred = credentials.Certificate("path/to/serviceAccountKey.json")
firebase_admin.initialize_app(cred)
